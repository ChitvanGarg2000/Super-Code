services:
  web:
    build:
      context: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: "production"
      # Recommended for Auth.js/NextAuth when behind a proxy / in containers
      AUTH_TRUST_HOST: "true"
    restart: unless-stopped
